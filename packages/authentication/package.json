{
  "name": "@fflamingo/authentication",
  "version": "0.0.1",
  "author": "Leonardo Ascione <leonardo.ascione@gmail.com>",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "babel src -w -d lib --copy-files -x '.ts,.tsx' --root-mode upward",
    "build": "babel src -d lib --copy-files -x '.ts,.tsx' --root-mode upward",
    "check-types": "tsc --noEmit",
    "test": "jest"
  },
  "peerDependencies": {
    "typeorm": "^0.2.15"
  },
  "devDependencies": {
    "typeorm": "^0.2.15"
  }
}
